# AR 卡牌对战原型 · 使用说明

## 文件清单

| 文件 | 作用 |
|---|---|
| `index.html` | 最终版，直接拖到 Netlify Drop 部署 |
| `test-card.png` | 我生成的测试卡（可打印/手机全屏显示，立刻验证整条链路） |
| `targets.mind` | 测试卡对应的识别目标文件，与测试卡配套使用 |
| `monster.glb` | 怪物模型（可选，想完全自托管时与 index.html 一起拖入 Netlify） |

> 说明：在你桌面上没有找到原来的 `index.html`（桌面的「小一梦」文件夹是空的）。不影响使用——下面是完整可用的新版文件；如果你需要我直接改你原来的文件，把路径发我即可。

---

## 一、先解决「方块太大」（针对你原来的 AR.js 版本）

你原来红色方块 `scale="1"` 时边长等于标记边长，所以撑满屏幕。标准 AR.js 模板只需把标记内的内容替换为：

```html
<a-marker preset="hiro">
  <!-- 底部光环 -->
  <a-ring radius-inner="0.22" radius-outer="0.30" color="#00E5FF"
          position="0 0.01 0" rotation="-90 0 0"
          material="side: double; transparent: true; opacity: 0.9"></a-ring>
  <!-- 缩小后的方块：0.3 = 标记边长的 30%，0.15 让方块底面正好踩在标记上 -->
  <a-box position="0 0.15 0" scale="0.3 0.3 0.3"
         material="color: #4ADE80; metalness: 0.2; roughness: 0.4;"></a-box>
</a-marker>
```

验证通过后，继续往下做自定义卡片识别（最终版 `index.html` 已经包含以上全部改动）。

---

## 二、自定义卡片识别（MindAR，可识别任意图片）

用 **MindAR 图像跟踪**替换 Hiro 标记：任意特征丰富的图片都能当识别目标，不再限于二维码样式的 Hiro。

### 1. 编译你的卡片图片 → targets.mind（唯一需要手动做的一步）

1. 打开官方在线编译工具：<https://hiukim.github.io/mind-ar-js-doc/tools/compile/>
2. 把你的卡片图片（JPG/PNG）拖进页面
3. 点击 **Start**，等待编译完成
4. 点击 **Download compiled**，得到 `targets.mind`
5. 把下载的文件**改名为 `targets.mind`**（保持与 `index.html` 中 `imageTargetSrc: ./targets.mind` 一致），与 `index.html` 一起部署

**卡片图片要求**（决定识别成功率）：
- 特征丰富：有文字、图案、照片、色块对比，避免大面积纯色
- 避免对称、重复花纹（例如棋盘格）——对称图案会导致方向混淆
- 尽量用打印版（哑光纸、A6 大小左右），或用另一台设备全屏显示
- 扫描时光线充足、卡片尽量放平

### 2. 想先快速验证？用现成的测试卡

我生成的 `test-card.png` 已编译成配套的 `targets.mind`。打印 `test-card.png`（或手机全屏显示），部署后直接扫它，怪物就会出现。验证通了再换你自己的卡牌图片。

---

## 三、部署到 Netlify Drop

1. 打开 <https://app.netlify.com/drop>
2. 把 **`index.html` + `targets.mind`** 一起拖进页面（两个文件同时拖）
3. 等待部署完成，会得到一个 `https://xxxx.netlify.app` 地址
4. 手机 Chrome/Safari 打开该地址，**允许摄像头权限**
5. 把卡片放入画面 → 绿色怪物踩着光环出现，并播放走路动画

> 可选：模型默认从 CDN 加载（无需额外文件）。如果想把模型也自托管，把 `monster.glb` 一起拖进 Netlify Drop，并把 `index.html` 里的模型地址改为 `./monster.glb`。

---

## 四、效果参数速查

| 想调什么 | 改哪里 |
|---|---|
| 怪物大小 | `<a-entity>` 上的 `scale="0.3 0.3 0.3"`（0.3 ≈ 卡宽的 1/3） |
| 怪物高度 | `position="0 0.24 0"`（0.24 使脚底贴卡面；模型脚底在 Y=-0.80） |
| 怪物颜色 | 代码底部 `tint-model` 组件里的 `color: '#63B24A'` 和 `emissive` 色值 |
| 光环颜色/大小 | `a-ring` 的 `color`、`radius-inner`、`radius-outer` |
| 走路动画 | 删掉 `animation-mixer="clip: walk; loop: repeat"` 即静止 |
| 识别灵敏度 | `mindar-image` 上的 `filterMinCF`（越小越灵敏）、`missTolerance`（越大越不易丢） |
| 换模型 | 改 `<a-asset-item id="monster" src="...">` 为你的 GLB 地址 |

**怪物模型来源**：低多边形僵尸鼠，来自 GitHub 仓库 `bsaleh1/ZombieGame`，MIT 许可（可自由使用/修改/商用，保留版权声明即可）。自带 walk 骨骼动画，约 60KB，适合移动端。CDN 直链已确认支持跨域：
`https://cdn.jsdelivr.net/gh/bsaleh1/ZombieGame@main/monster.glb`

---

## 五、常见问题

| 现象 | 原因与处理 |
|---|---|
| 黑屏、相机不启动 | 页面必须 HTTPS（Netlify 已满足）；检查浏览器是否拒绝了摄像头权限；iOS 首次访问需在地址栏确认允许 |
| 一直转圈 | 网络加载 CDN 脚本慢，刷新重试 |
| 识别不到卡片 | 图片特征太少/光线太暗/卡片太小或太远；换更丰富的图重新编译 |
| 怪物太大/太小 | 改 `scale`（0.2 更小、0.4 更大） |
| 怪物浮空/陷进卡面 | 改 `position` 的 Y 值（调大浮高、调小贴地） |
| 怪物是黑色 | 检查控制台是否有 `tint-model` 报错；确认加载的是本文件的完整代码 |
| 想用回 Hiro 标记 | 把 `mindar-image` 的 `imageTargetSrc` 换成 AR.js 方案即可，代码结构不变 |

---

## 六、技术栈与验证情况

- 技术栈：A-Frame 1.4.1 + MindAR 1.2.5（图像跟踪）+ aframe-extras 6.1.1（骨骼动画播放）
- 全部依赖走 CDN，已逐一验证可用；模型已本地解析验证（GLB 格式有效、含 walk 动画、包围盒 1.6×2.5×2.0）
- 已在本机 Chrome 实测：组件注册、目标文件加载、模型渲染（翠绿上色 + 走路动画 + 光环）均正常
- 相机与真实扫描需在手机 + Netlify HTTPS 环境下最终确认
